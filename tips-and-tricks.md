# Check your environments in Anaconda

`conda info --envs`

# Update Anaconda

`conda update conda`

# Jupyter cell run

- Press `Shift + Enter` to execute selected cell and jump to the next one
- Press `Ctrl + Enter` to execute selected cell and stay there
